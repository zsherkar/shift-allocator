# Repository Cleanup — 19 September 2026

## Branch Reconciliation

The renamed repository is `zsherkar/shift-allocator`. The default branch previously lacked the production allocation optimizer, membership handling, response recovery, and snapshots. The cleanup merges the deployed `render-monthly-hosting` history into `master`, resolving the API package and security-report conflicts with the tested production versions. Before cleanup edits, the reconciled tree matched production commit `335de44` exactly.

No allocation rules, API routes, authentication controls, or database schemas were changed by the cleanup. The local working checkout with unrelated changes was left untouched; work was done in an isolated worktree.

## Removed Code and Assets

- The empty `artifacts/mockup-sandbox` workspace and its duplicated UI starter kit. No application imports it, and it contains no actual mockup components.
- 44 unreferenced frontend UI components and 33 direct dependencies used only by that starter code. A TypeScript import traversal from the real app entrypoint identified the reachable modules; no dynamic imports or runtime `require` calls in the frontend supplied alternate entrypoints. The retained app was then built, tested, and inspected in a browser.
- The obsolete `ScheduleCalendar.tsx`; the app imports `ExcelScheduleCalendar.tsx` instead.
- The unused `use-mobile.tsx` helper, formerly used by the removed sidebar starter component.
- The unused hero and Open Graph images and their old design requirements file; none was referenced by the current app or HTML entrypoint.
- `README_PREVIEW.html` and its custom preview server, which duplicated an older README.
- The scripts package's hello-world sample, its unused TypeScript configuration/dependencies, and an unreferenced post-merge script with an outdated database package filter. Production startup and smoke scripts remain.

The dependency graph decreased from 574 to 505 audit entries. The generated application CSS decreased from approximately 107.5 KB to 55.1 KB. The main JavaScript bundle remains large because exports and application libraries are still included; splitting it was outside this cleanup.

## GitHub Housekeeping

- Dependabot PR #19 proposed esbuild 0.28.1. Both active branches already had 0.28.2 through #20/#21, so #19 was closed as superseded.
- Historical Codex branches were reviewed for ancestry. Eight were already merged; `codex/fairer-august-allocation` was patch-equivalent to a production commit (`git cherry` marked its only unique commit with `-`). Their work is retained in repository history.
- The closed Dependabot branch contains only the superseded dependency update and remains recoverable through PR #19.
- Keep `master` and `render-monthly-hosting`. Local worktrees and their branches were retained, including older working directories, because they may contain ongoing local work.

Remote branch tips retained here for traceability. The older draft-build-and-deployment-plan-treko5 and Dependabot branches were already absent on the final fetch; the following eight obsolete remote branches were retired during cleanup. Local branches and worktrees remain.

| Retired remote branch | Last commit |
| --- | --- |
| `codex/draft-build-and-deployment-plan` | `e51255efe1c7148f5d9d524c697135e335ab3d5e` |
| `codex/draft-build-and-deployment-plan-7cq65r` | `d31dda40a80c6f14c5f94e96f7c781e3542f5e7c` |
| `codex/fairer-august-allocation` | `951da6fc19020eae3466ff2303ea66292024f7e6` |
| `codex/fix-production-security-alerts-2026-09` | `527d088b68f3d3056617ff2186bb8f56aba3c6c7` |
| `codex/fix-security-alerts-2026-09` | `46d4da29dfd7c0573bb225664b8e84e884875fea` |
| `codex/implement-feedback-for-i-house-shift-allocator` | `23fca607d59e04c1c3a8160281d5ae2698e805c8` |
| `codex/redesign-ui-to-victorian-style-theme` | `ffac894232ed755a8459dff1204bd26a677d936e` |
| `codex/update-admin-dashboard-features` | `925e5917a059e2c84f14d947f5e415696d97aec0` |

## Verification

- Workspace install and lockfile update succeeded.
- Full typecheck and frontend/API production builds passed.
- All 91 existing tests passed, including the six security regression tests.
- `pnpm audit --json` reported zero vulnerabilities after dependency removal.
- The documentation preview uses real components with explicitly fictional fixtures. Browser inspection covered public availability selection, responses, statistics, allocation audit, recovery controls, and the calendar; the actual PNG export was captured.
- Media provenance and reproduction instructions are in [media.md](media.md).

Historical entries in `Updates.md` retain their original local paths and service names. Existing Render service/database identifiers are intentionally unchanged by the repository rename.
